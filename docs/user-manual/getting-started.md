# 1. Getting Started

> **Status: placeholder.**

To be written: how to install the app, what appears the first time it opens, and how to sign in or create an account.

Intended scope:

- **Installing the app** — where to get the package and what to allow during installation.
- **The first screen** — the app opens locked, at the sign-in screen, every time it is started.
- **Signing in** — entering a username and a password, and what happens if either is wrong.
- **Creating an account** — the fields the form asks for, the rules each one must meet, adding a photo and tags, and what happens once the account is created.
- **What is stored on the device** — a short note that the account and everything in it stay on the phone.

## Screenshots to include

<div class="phone-shots">
  <figure>
    <img src="/static/manual/chapter1-image1.jpg" alt="The sign-in screen" />
    <figcaption>The sign-in screen, as it appears when the app opens</figcaption>
  </figure>
  <figure>
    <img src="/static/manual/chapter1-image2.jpg" alt="The account creation form" />
    <figcaption>The account creation form</figcaption>
  </figure>
  <figure>
    <img src="/static/manual/chapter1-image3.jpg" alt="An error shown under a field" />
    <figcaption>An entry that is not accepted, with the reason shown</figcaption>
  </figure>
</div>

Place image files in `docs/public/static/manual/` and reference them as `/static/manual/<name>.png`. Keep each `figure` inside the `phone-shots` block so the images sit side by side at a readable size.
