# 1.4 References

> **Status: placeholder.**

To be written: the documents on which this specification depends.

Intended scope:

- **[RAD — Requirements Analysis Document](../../rad/overview)** — the requirements the specified classes ultimately serve.
- **[SDD — System Design Document](../../sdd/overview)** — the subsystems the specified classes realise, and the services whose operations they implement.
- The external libraries whose interfaces this design depends on, each with the version the specification assumes.
