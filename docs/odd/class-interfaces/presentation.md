# 3.1 Presentation

> **Status: placeholder.**

To be written: the classes of the Presentation package — the screens, the reusable widgets they are built from, the navigation controller and the scope through which it is reached, and the classes holding the visual conventions and the fixed text.

Source: `features/`, `shared/widgets/`, `core/theme/`, `core/constants/`.
