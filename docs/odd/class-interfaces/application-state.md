# 3.2 Application State

> **Status: placeholder.**

To be written: the classes of the Application State package — the stores owning the account, the personal profile, the privacy preferences, the saved items, the conversations, the trip catalogue and the search mode.

Each entry is to state the state the class owns, how a change to it is published, and the contractual property established in [SDD 4](../../sdd/subsystem-services): an operation that changes state returns as soon as the change is visible, not when it has been stored.

Source: `shared/state/`.
