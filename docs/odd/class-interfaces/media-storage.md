# 3.7 Media Storage

> **Status: placeholder.**

To be written: the two classes of the Media Storage package — the one copying a chosen file into the application's private storage and yielding the reference by which it is thereafter found, and the adapter obtaining that file from the device's gallery.

The separation between them is a design decision recorded in [SDD 4](../../sdd/subsystem-services), and the entries are to make it visible in the interfaces themselves.

Source: `features/profile/image/`.
