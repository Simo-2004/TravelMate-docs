# 3.4 Persistence

> **Status: placeholder.**

To be written: the classes of the Persistence package, at the two levels distinguished in [SDD 4](../../sdd/subsystem-services) — the data sources deciding where each kind of data lives, and the repositories translating between domain objects and stored rows and applying encryption.

The data-source interfaces and their implementations are to be specified separately, since it is the interface on which Application State depends.

Source: `shared/data/`.
