# 3.3 Domain Logic

> **Status: placeholder.**

To be written: the classes and functions of the Domain Logic package — trip and companion ranking, reply selection, trip-proposal matching, registration validation and label normalisation — together with the domain objects on which they operate.

Every operation in this package is a pure function, so each entry is to state its result in terms of its arguments alone, and the domain objects are to be specified as the immutable values they are.

Source: `shared/utils/`, `shared/models/`.
