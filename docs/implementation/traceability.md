# 4.3 Design-to-Code Traceability

> **Status: placeholder.**

To be written: the correspondence between the interfaces specified in [ODD 3](../odd/class-interfaces/) and the files of the source, established so that no specified element is unimplemented and no implemented element is unspecified.

Intended scope:

- **The rule of correspondence** — how the location of a class in the source follows from the package it belongs to in [ODD 2](../odd/packages).
- **Traceability tables** — for each package, the specified element and the file realising it.
- **Interfaces and their realizations** — where a declared interface and its concrete class are located relative to each other.
- **Verification in both directions** — how it is established that the source contains nothing the specification does not, and omits nothing it requires.
