# 4.4 Build & Execution Process

> **Status: placeholder.**

To be written: the procedure transforming the source into a running application, stated as the sequence of commands performing it.

Intended scope:

- **Dependency retrieval** — the command obtaining the external libraries, and the option constraining it to the recorded versions.
- **Execution during development** — the command running the application on a device or emulator.
- **Production build** — the command producing a distributable artefact for each target platform.
- **Test execution** — the command running the test suite and producing the coverage report consumed by [4.5](./quality-criteria).
- **Generated sources** — whether any part of the source is generated, and the command generating it.
